{
  "name": "@miden/private-transport-client-web",
  "version": "0.1.0",
  "description": "Miden Private Transport Web Client",
  "type": "module",
  "main": "./dist/index.js",
  "browser": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && cross-env RUSTFLAGS=\"--cfg getrandom_backend=\\\"wasm_js\\\"\" rollup -c rollup.config.js",
    "build-dev": "npm install && MIDEN_WEB_DEV=true npm run build",
    "test": "yarn build && yarn playwright install --with-deps && yarn playwright test",
    "test:clean": "yarn install && yarn playwright install --with-deps && MIDEN_WEB_DEV=true yarn run build && yarn playwright test",
    "test:server": "npm run build && node app/server.js",
    "app": "npm run build && node app/server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@rollup/plugin-commonjs": "^25.0.7",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@types/node": "^22.4.1",
    "@wasm-tool/rollup-plugin-rust": "^3.0.3",
    "cross-env": "^7.0.3",
    "http-server": "^14.1.1",
    "rimraf": "^6.0.1",
    "rollup": "^3.27.2",
    "typescript": "^5.5.4"
  }
}
